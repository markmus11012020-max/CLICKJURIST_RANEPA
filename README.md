# ClickJurist Production

Юридический ИИ-сервис: двухэтапный мегапайплайн (LLM-1 → LLM-2) с анонимностью данных
(маскировка ПДн в изолированном контуре РФ), веб-фактчекингом, guardrails и оплатой через Робокассу.

Полное ТЗ: [prompt.150926.md](prompt.150926.md) · [prompt160926.md](prompt160926.md) · [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

---

## Архитектура за 30 секунд

```
Клиент (SPA) ─▶ POST /api/query/async (202 Accepted + task_id)
                     │
        ┌─────────────┼──────────────────────────┐
        ▼             ▼                          ▼
 JWT-cookie    STAGE 1 (контур РФ)        Web-фактчекинг (агент)
 + Paywall     YandexGPT/Ollama/regex      Yandex/Serper/Tavily
        │             │                          │ приоритет consultant.ru
        └─────────────┴────────────┬─────────────┘
                                   ▼
                        STAGE 2 (внешний)
                        AITunnel: LLM-1 → LLM-2
                                   │
                                   ▼
                        Guardrails → Дисклеймер → клиенту

Клиент опрашивает статус:
    GET /api/query/status/{task_id}     — polling
    GET /api/query/stream/{task_id}     — SSE-стриминг событий
```

---

## Структура репозитория

```
.
├── backend/                       # FastAPI + бизнес-логика
│   ├── main.py                    # HTTP-слой, эндпоинты, middleware
│   ├── config.py                  # Настройки (env + KMS bootstrap)
│   ├── db.py                      # SQLite/PostgreSQL (No-Data-Retention)
│   ├── models.py                  # Pydantic-схемы запросов/ответов
│   ├── security.py                # 152-ФЗ: SHA-256 идентификация сессий
│   ├── jwt_auth.py                # JWT-авторизация (HttpOnly cookies)
│   ├── task_store.py              # Фоновые задачи + SSE-стриминг
│   ├── logging_setup.py           # Zero-Storage Logging + PII-фильтр
│   ├── yandex_kms.py              # Шифрование секретов через Yandex KMS
│   └── services/
│       ├── pii_masker.py          # STAGE 1: маскировка ПДн
│       ├── web_factcheck.py       # Web-поиск (агент + приоритет доменов)
│       ├── llm_chain.py           # Мегапайплайн + failover
│       ├── prompts.py             # Системные промпты (LLM-1/LLM-2/checklist + 4 PROMPT_* для документов)
│       ├── providers.py           # AITunnel / YandexGPT / Ollama
│       ├── robokassa.py           # Эквайринг (создание счёта, подписи)
│       ├── pdf_generator.py       # PDF с кириллицей (reportlab)
│       └── guardrails.py          # Контроль галлюцинаций (валидация)
├── frontend/                      # SPA (HTML/CSS/JS, без зависимостей)
├── tests/                         # pytest: безопасность, пайплайн, платежи, async, guardrails
├── deploy/yandex-cloud/           # Dockerfile и инструкции для Cloud
├── docs/                          # Архитектура, эксплуатация, безопасность
├── data/                          # SQLite-БД (только development)
├── secrets/                       # Шифротексты KMS (production)
├── .env.example                   # Шаблон конфигурации (16 разделов)
├── prompt.150926.md               # Исходное ТЗ (MVP)
├── prompt160926.md                # ТЗ на production-ready доработки
└── start.bat                      # Автоматический деплой (Windows)
```

---

## Быстрый старт (локально)

```bash
python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\Activate.ps1
pip install -r backend/requirements.txt
cp .env.example .env
python -m backend.main
```

Сервис поднимется на http://127.0.0.1:8001. Документация API: http://127.0.0.1:8001/api/docs.

**Windows (автоматически):** достаточно запустить `start.bat` — скрипт
остановит старые процессы, очистит кэш, создаст/активирует venv,
установит зависимости и запустит сервер.

---

## Фирменный стиль интерфейса

Логотип: геральдический щит с весами правосудия (inline SVG, тонкие линии) + «КликЮрист»
(шрифт Manrope ExtraBold, мягкий межбуквенный интервал). Favicon синхронизирован с эмблемой.

Палитра (`frontend/style.css`, CSS-переменные `:root`):

| Токен | Значение | Назначение |
|-------|----------|------------|
| `--bg-0 / --bg-1 / --bg-2` | `#111625 / #141a29 / #182036` | Шёлковый матово-синий фон, без едкой черноты |
| `--accent / --accent-strong` | `#10a3ba / #0a819c` | Приглушённый матовый бирюзово-циановый акцент |
| `--accent-deeper / --accent-soft` | `#0a5f70 / #7fc4d2` | Глубина градиентов, мягкая матовая бирюза |
| `--text / --text-soft / --text-mute` | `#e4e9f2 / #a9b4c9 / #737f97` | Приглушённый, легко читаемый текст |

Плашки результатов оформлены в духе glassmorphism: полупрозрачный фон,
`backdrop-filter: blur(14px)` и едва заметное внутреннее неоново-голубое свечение.
JS-логика (запросы к FastAPI, подгрузка чек-листов, выбор одной из 4 кнопок типа
документа через классы `primary`/`ghost` и `aria-pressed`) не затронута.

---

## Отказоустойчивость (failover)

Пайплайн построен на трёх провайдерах LLM и гарантирует ответ даже при
падении одного из внешних API:

| Этап         | Основной провайдер      | Резервный (failover)         |
|--------------|------------------------|------------------------------|
| Stage 1      | `PRIMARY_PROVIDER`       | второй провайдер             |
| Stage 2      | `yandex`               | `aitunnel` (Gemini, всегда) |
| Черновик/Эталон | `PRIMARY_PROVIDER`   | второй провайдер             |

- Переключение между `PRIMARY_PROVIDER=aitunnel` и `PRIMARY_PROVIDER=yandex`
  задаётся в `.env`.
- Stage 2 использует специальную цепочку `stage2_fallback_chain()`,
  которая гарантирует, что `aitunnel` (Gemini 2.5 Flash) всегда
  является финальным fallback — даже если Yandex упал (например,
  `UnicodeEncodeError` из-за кириллицы в заголовках).
- Защита HTTP-заголовков: все значения заголовков проходят через
  `_ascii_safe()` — кириллица и non-ASCII символы удаляются до
  отправки, предотвращая `UnicodeEncodeError` в библиотеке `requests`.

---

## Безопасность (152-ФЗ)

- **Идентификация сессии:** `SHA-256(IP + fingerprint + SESSION_HASH_SALT)`, соль в KMS.
- **JWT-авторизация:** токен в `HttpOnly, Secure, SameSite=Strict` cookie.
  Сессия НЕ привязана к IP — переключение Wi-Fi ↔ LTE не рвёт сессию.
- **В логах** — только анонимизированный UUID; сырые IP и текст запросов не сохраняются.
- **Персональные данные** маскируются российским LLM либо regex-страховкой **до** внешней модели.
- **Guardrails:** проверка диапазонов статей кодексов, запрет выдуманных цифр/номеров дел,
  контроль сохранности плейсхолдеров.
- **Секреты** шифруются Yandex KMS (production) или хранятся в `.env` (development).

---

## Тарифы (по умолчанию)

| Услуга                                          | Цена   |
|-------------------------------------------------|--------|
| Юридическая консультация                        | 99 ₽   |
| Чек-лист действий                               | 100 ₽  |
| Шаблон документа / PDF-версия                   | 300 ₽  |
| **Пакет «Решение проблемы под ключ» (basic)**   | 390 ₽  |
| **Пакет «Решение проблемы под ключ» (premium)** | 490 ₽  |

Первый запрос — бесплатный. Пакетный тариф открывает доступ сразу к консультации,
чек-листу и (для premium) PDF-документу в рамках одной сессии.

---

## Тесты

```bash
pip install pytest
pytest -q
```

Покрывают: безопасность, маскировку ПДн, мегапайплайн в оффлайн-режиме, подписи Robokassa,
JWT-авторизацию, фоновые задачи, guardrails, веб-фактчекинг, динамические оговорки.

---

## Качество генерации (LegalTech-стандарт)

Все системные промпты заточены под юридически применимый результат:

- **Локализация суда:** модель жёстко извлекает город/субъект из обезличенного запроса и пишет точное наименование («Арбитражный суд Самарской области», «Октябрьский районный суд г. Самары» и т. п.).
- **Сроки и нормы:** для корпоративных споров указывается срок давности 1 год (ст. 181 ГК РФ), для оспаривания решений общих собраний ООО — 3 месяца (ст. 43 ФЗ «Об ООО»).
- **Динамические оговорки:** к каждому ответу добавляется юридическая оговорка о том, что сроки исчисляются с момента, когда лицо узнало или должно было узнать о нарушении (ст. 181, 196 ГК РФ, ст. 392 ТК РФ).
- **Запрет мета-языка:** модель не пишет «Укажите…», «Определите…» в адрес клиента; каждая строка чек-листа — готовое императивное действие.
- **Специализированные шаблоны документов:** для каждого типа (`lawsuit`, `claim`, `complaint`, `court_order_cancellation`) — свой системный промпт с развёрнутым правовым обоснованием (ст. 53.1 ГК РФ, ст. 45–46 ФЗ «Об ООО», ст. 10 ФЗ «Об ООО», ст. 167–168 ГК РФ).
- **Чистый бланк:** в шаблонах документов запрещены угловые скобки — только профессиональные прочерки `________________________` для ручного заполнения.
- **Расширенные лимиты:** `MAX_TOKENS_DOCUMENT=8000`, `MAX_TOKENS_CHECKLIST=4000` (см. `.env.example`) — модель не обрывает генерацию длинного иска/претензии.
- **Без фразы «обратитесь к юристу»:** вместо этого модель рекламирует функционал сервиса (Шаг 2 / Шаг 3).

---

## Production-ready доработки (prompt160926.md)

| Раздел ТЗ | Реализация |
|-----------|------------|
| **2.1 Асинхронная генерация** | `POST /api/query/async` → `202 Accepted` + `task_id`. Polling через `GET /api/query/status/{task_id}`. SSE-стриминг через `GET /api/query/stream/{task_id}`. Бэкенд: `backend/task_store.py` (in-memory) + опционально Celery + Redis (`TASK_BACKEND=celery`). |
| **2.2 Агентский веб-фактчекинг** | `backend/services/web_factcheck.py`: `agent_search()` генерирует 2–3 запроса из обезличенного резюме, парсит топ-3 результатов, очищает HTML, приоритизирует домены `consultant.ru`, `garant.ru`, `pravo.gov.ru`. |
| **3.1 JWT-авторизация** | `backend/jwt_auth.py`: токен в `HttpOnly, Secure, SameSite=Strict` cookie. Сессия НЕ привязана к IP — переключение Wi-Fi ↔ LTE не рвёт сессию. Эндпоинты: `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/status`. |
| **3.2 Улучшение маскировки ПДн** | Расширенные regex-паттерны в `backend/services/pii_masker.py` (организации, ФИО с инициалами, банковские реквизиты). Двухслойная защита: LLM + regex-страховка. |
| **4.1 Быстрые пресеты** | `frontend/index.html`: блок `.quick-tags` под формой ввода. 4 пресета: «Затопили соседи сверху», «Продавец отказывается вернуть деньги», «Оспаривание штрафа ГИБДД», «Задержка зарплаты». Клик подставляет структурированный мини-шаблон. |
| **4.2 Адаптивность и аккордеоны** | `frontend/style.css`: media-запросы для `<480px` (уменьшенные шрифты, компактные отступы). Подвал: `<details>`-аккордеоны для блоков 152-ФЗ и дисклеймера. |
| **5.1 Динамические оговорки** | `backend/services/prompts.py::with_dynamic_disclaimer()` — добавляет оговорку о сроках исковой давности (ст. 181, 196 ГК РФ, ст. 392 ТК РФ). |
| **5.2 Guardrails** | `backend/services/guardrails.py`: проверка диапазонов статей кодексов, запрет выдуманных цифр/номеров дел, контроль сохранности плейсхолдеров. При критических нарушениях — авто-регенерация. |
| **6.1 Пакетный тариф** | `POST /api/package` — тариф «Решение проблемы под ключ»: `basic` (390 ₽) = консультация + чек-лист; `premium` (490 ₽) = + шаблон документа. |
| **7 Тестирование** | 61 автотест (pytest): безопасность, маскировка, пайплайн, платежи, JWT, фоновые задачи, guardrails, веб-фактчекинг, динамические оговорки. |

## Финальная UX-доработка (prompt170926.md)

| Шаг ТЗ | Реализация |
|--------|------------|
| **1. Streaming + Skeleton + Таймер** | Бэкенд: `backend/services/providers.py::AITunnelProvider.stream_chat()` — SSE-стриминг токенов через OpenAI-совместимый протокол (`stream: true`). `backend/services/llm_chain.py::run_pipeline_streaming()` — генератор событий `token`/`sources`/`meta`/`completed`. Фронтенд: `frontend/script.js::streamConsultation()` — `EventSource` рендерит токены в `queryResultBody` по мере поступления. Skeleton Loader (`.skeleton-loader`) + таймер «Прошло N сек.» (`.result-timer`). |
| **2. 6 тегов 50/50 B2C/B2B** | `frontend/script.js::QUICK_TAG_TEMPLATES` — 3 B2C-тега («Возврат товара и денег», «Нарушения трудового права», «Оспаривание штрафа ГИБДД») + 3 B2B-тега («Взыскание дебиторской задолженности», «Оспаривание решения общего собрания участников ООО», «Экспертный аудит B2B-контракта»). Каждый тег подставляет развёрнутый структурированный мини-шаблон с плейсхолдерами `[ORG_1]`, `[INN_1]`, `[CASE_1]`. |
| **3. Свободный доступ к Шагам 2/3** | Блокировка зависимых шагов снята. Все секции (`#checklist`, `#document`) и кнопки внутри них (`#runChecklistBtn`, `#runDocumentBtn`, `.doc-type-btn`) полностью активны сразу после загрузки страницы: `disabled` отсутствует, `opacity: 1`, `pointer-events: auto`. Классы `.step-locked` / `.step-unlocked` и функции `lockDependentSteps()` / `unlockDependentSteps()` удалены из `frontend/script.js`, `frontend/style.css` и `frontend/index.html`. |
| **4. Пакетный тариф + Дисклеймер** | Кнопка «Всё включено (Консультация + Чек-лист + Документ) за 390 ₽» в `frontend/index.html` (`.package-btn`) с интеграцией Робокассы через `POST /api/payment/create`. Дисклеймер ИИ вынесен в нижнюю часть Шага 3 (`.step-disclaimer`). |
| **5. 152-ФЗ аккордеон + контраст + мобильная адаптация** | Аккордеон «Как мы защищаем ваши персональные данные (152-ФЗ)» наполнен тремя разделами: Zero-Storage, локальный контур Stage 1, JWT-сессии. Контрастность текста в аккордеонах повышена (`color: var(--text)`). На экранах `<480px` 6 тегов перестраиваются в 2 ряда по 3, пакетная кнопка компактнее, бейдж блокировки уменьшен. |

---

## Документация

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — поток данных и схема БД.
- [docs/OPERATIONS.md](docs/OPERATIONS.md) — запуск, переменные, мониторинг.
- [docs/SECURITY.md](docs/SECURITY.md) — соответствие 152-ФЗ.
- [docs/ROADMAP.md](docs/ROADMAP.md) — план развития.
- [deploy/yandex-cloud/README.md](deploy/yandex-cloud/README.md) — production-развёртывание.
- [docs/AITUNNEL_API_RULES.md](docs/AITUNNEL_API_RULES.md) — правила работы с AITunnel API.

---

## Лицензия

Проект разработан в рамках частной инициативы ClickJurist. Использование — по согласованию с правообладателем.