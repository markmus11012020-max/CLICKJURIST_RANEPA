# ==============================================================================
# ClickJurist Production — зависимости Python 3.11
# ==============================================================================

# --- Web / API ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# --- Конфигурация и валидация ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# --- HTTP-клиент (AITunnel, YandexGPT, Ollama, Web-поиск, KMS) ---
requests>=2.31.0

# --- PDF с поддержкой кириллицы ---
reportlab>=4.1.0

# --- Production-хранилище (PostgreSQL в Yandex Cloud) ---
# Обязательно для APP_ENV=production; локально используется встроенный sqlite3.
psycopg[binary]>=3.1.18

# --- JWT-авторизация (раздел 3.1 ТЗ prompt160926.md) ---
PyJWT>=2.8.0

# --- Celery + Redis для масштабирования фоновых задач (раздел 2.1 ТЗ) ---
# Опционально: устанавливать только при TASK_BACKEND=celery.
# celery>=5.3.0
# redis>=5.0.0

# --- Locust для нагрузочного тестирования (раздел 7 ТЗ) ---
# Опционально: устанавливать только для нагрузочных тестов.
# locust>=2.20.0
